SELECT DISTINCT name FROM people LEFT JOIN directors ON directors.person_id = people.id LEFT JOIN movies ON movies.id = directors.movie_id LEFT JOIN ratings ON ratings.movie_id = movies.id WHERE ratings.rating >= 9.0;